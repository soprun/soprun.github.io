domain: soprun.com       # if you want to force HTTPS, specify the domain without the http at the start, e.g. example.com
url: https://soprun.com  # the base hostname and protocol for your site, e.g. http://example.com
theme: jekyll-theme-minimal
