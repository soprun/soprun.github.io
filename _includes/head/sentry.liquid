{% if jekyll.environment == "production" and site.env.SENTRY_DSN %}
    <script src='https://js.sentry-cdn.com/5943bcec0a2e4787882cbb988fd0aabc.min.js' async crossorigin="anonymous"></script>
    <script>
        Sentry.onLoad(function () {
            Sentry.init({
                dsn: "{{ site.env.SENTRY_DSN }}",
                release: "{{ site.env.CF_PAGES_COMMIT_SHA }}",
                environment: "{{ jekyll.environment }}",
            });
        });
    </script>
{% endif %}